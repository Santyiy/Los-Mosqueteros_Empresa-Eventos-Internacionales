Después de completar la refactorización de la Capa de Servicio, y ahora el EventControllerllama a EventService, que a su vez se comunica con el repositorio. La arquitectura del Backend para eventos está limpia. 
El problema actual es que, a pesar de que el endpoint de Java funciona, el Frontend ( index.html) sigue mostrando los datos falsos del archivo mock_events.json. Mi plan de hoy es hacer la conexión final. 
En Visual Studio Code, lo abrirá index_logic.js.
Eliminaré la lógica de lectura del archivo JSON Mock y descomentaré/restauraré la llamada fetcha http://localhost:8080/api/v1/events. Una vez implementado, hará una prueba de integración, asegurándome de que tanto el servidor de Java como XAMPP estén activos. 
El resultado esperado es que la página web muestrea por primera vez la lista de eventos reales provenientes de la Base de Datos MySQL. Mi pregunta es si debo también eliminar la dependencia de Mock Data de la autenticación ( auth_logic.js) hoy, o si me concentro solo en los eventos
